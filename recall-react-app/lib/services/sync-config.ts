// Auto-tagging configuration for video sync
// Re-export from shared package for backward compatibility

export { TAG_RULES, detectTags, getTagColor } from "@shared"
export type { TagRule } from "@shared"
